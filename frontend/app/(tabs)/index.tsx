import { Ionicons } from "@expo/vector-icons";
import { useRouter } from "expo-router";
import { Image, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { BranchSelector } from "@/src/components/branch-selector";
import { FoodCard, ScreenTitle } from "@/src/components/ui";
import { isBranchOpen } from "@/src/data/branches";
import { categories } from "@/src/data/menu";
import { foodImages } from "@/src/data/images";
import { translate } from "@/src/i18n";
import { useApp } from "@/src/store/app-store";
import { makeStyles, useTheme } from "@/src/theme";

export default function HomeScreen() {
  const router = useRouter(); const insets = useSafeAreaInsets(); const { colors } = useTheme(); const styles = useStyles();
  const { language, menu, menuLoading, session, selectedBranch, canOrder, updates } = useApp();
  if (!selectedBranch) return <View style={[styles.root, styles.branchRoot, { paddingTop: insets.top + 26 }]}><BranchSelector required /></View>;
  const open = isBranchOpen(selectedBranch);
  const canStartOrder = canOrder && open;
  return <View style={[styles.root, { paddingTop: insets.top + 16 }]}><ScrollView contentContainerStyle={{ paddingBottom: 34 }} showsVerticalScrollIndicator={false}>
    <View style={styles.header}><View><Text style={styles.logo}>DSB <Text style={styles.logoAccent}>FAST FOOD</Text></Text><Text style={styles.location}><Ionicons name="location" size={13} color={colors.brandPrimary} /> {selectedBranch.city}, {selectedBranch.state}</Text></View><Pressable accessibilityRole="button" accessibilityLabel="Open profile" onPress={() => router.push("/(tabs)/profile")} style={styles.avatar}><Ionicons name={session ? "person" : "person-outline"} size={20} color={colors.brandPrimary} /></Pressable></View>
    <View style={styles.branchWrap}><BranchSelector /></View>
    <View style={styles.greeting}><Text style={styles.eyebrow}>HOT & SPICY · SINCE 2025</Text><Text style={styles.headline}>{translate(language, "hello")}</Text><Text style={styles.subline}>{selectedBranch.isComingSoon ? `Opening on ${selectedBranch.openingDate}` : `Fast delivery in ${selectedBranch.city}`}</Text></View>
    <View style={styles.hero}><Image source={{ uri: foodImages.hero }} style={styles.heroImage} /><View style={styles.heroOverlay}><View style={[styles.statusPill, { backgroundColor: selectedBranch.isComingSoon ? colors.warning : open ? colors.success : colors.error }]}><View style={styles.statusDot} /><Text style={styles.statusText}>{selectedBranch.isComingSoon ? "COMING SOON" : open ? translate(language, "open") : translate(language, "closed")}</Text></View><Text style={styles.heroTitle}>Street food{`\n`}with soul.</Text><Text style={styles.heroCopy}>{selectedBranch.isComingSoon ? `We'll be opening on ${selectedBranch.openingDate}.` : open ? "Fresh wok-fired favourites, ready when you are." : `We reopen at ${selectedBranch.openingTime ?? "11:00"}. Plan your next craving.`}</Text>{canStartOrder ? <Pressable accessibilityRole="button" onPress={() => router.push("/(tabs)/menu")} style={({ pressed }) => [styles.heroButton, pressed && { opacity: 0.8 }]}><Text style={styles.heroButtonText}>{translate(language, "orderNow")}</Text><Ionicons name="arrow-forward" size={17} color={colors.onBrandPrimary} /></Pressable> : <View style={styles.closedCta}><Ionicons name={selectedBranch.isComingSoon ? "calendar-outline" : "time-outline"} size={16} color={colors.onBrandTertiary} /><Text style={styles.closedCtaText}>{selectedBranch.isComingSoon ? `Opening on ${selectedBranch.openingDate}` : `Opens at ${selectedBranch.openingTime ?? "11:00"}`}</Text></View>}</View></View>
    <View style={styles.infoRow}><View><Text style={styles.infoLabel}>{selectedBranch.openingTime && selectedBranch.closingTime ? `${selectedBranch.openingTime} – ${selectedBranch.closingTime} daily` : "Branch details coming soon"}</Text><Text style={styles.infoValue}>{selectedBranch.deliveryCharge !== undefined ? `₹${selectedBranch.deliveryCharge} delivery · Min ₹${selectedBranch.minimumOrder}` : "Ordering opens with this branch"}</Text></View><Ionicons name={selectedBranch.isComingSoon ? "calendar" : "flame"} size={24} color={colors.brandPrimary} /></View>
    {updates.length > 0 ? <><ScreenTitle eyebrow="FROM DSB" title="Restaurant updates" /><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.horizontalCards}>{updates.map((update) => <View key={update.id} style={styles.updateCard}>{update.image ? <Image source={{ uri: update.image }} style={styles.updateImage} /> : <View style={styles.updateAccent}><Ionicons name="megaphone-outline" size={24} color={colors.brandPrimary} /></View>}<Text style={styles.updateTitle} numberOfLines={2}>{update.title}</Text><Text style={styles.updateText} numberOfLines={2}>{update.description}</Text></View>)}</ScrollView></> : null}
    <ScreenTitle eyebrow="DSB FAVOURITES" title={translate(language, "popular")} right={canOrder ? <Pressable onPress={() => router.push("/(tabs)/menu")}><Text style={styles.link}>{translate(language, "browse")}</Text></Pressable> : null} />
    {menuLoading ? <Text style={styles.muted}>Loading today’s menu…</Text> : menu.length > 0 ? <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.horizontalCards}>{menu.slice(0, 3).map((item) => <FoodCard key={item.id} item={item} compact />)}</ScrollView> : <View style={styles.noMenu}><Ionicons name="restaurant-outline" size={25} color={colors.muted} /><Text style={styles.muted}>{selectedBranch.isComingSoon ? "Menu will be available when this branch opens." : "Menu is temporarily unavailable."}</Text></View>}
    <ScreenTitle eyebrow="FIND YOUR FAVOURITE" title={translate(language, "categories")} />
    <View style={styles.categoryGrid}>{categories.map((category, index) => <Pressable key={category} disabled={!canOrder} accessibilityRole="button" onPress={() => router.push({ pathname: "/(tabs)/menu", params: { category } })} style={({ pressed }) => [styles.category, !canOrder && styles.categoryDisabled, pressed && { opacity: 0.76 }]}><View style={[styles.categoryIcon, { backgroundColor: [colors.brandTertiary, colors.surfaceTertiary, colors.brandTertiary, colors.surfaceTertiary][index] }]}><Ionicons name={index === 0 ? "restaurant" : index === 1 ? "fast-food" : index === 2 ? "flame" : "cafe"} size={22} color={colors.brandPrimary} /></View><Text style={styles.categoryText}>{category}</Text></Pressable>)}</View>
  </ScrollView></View>;
}

const useStyles = makeStyles((colors) => StyleSheet.create({ root: { flex: 1, backgroundColor: colors.surface, paddingHorizontal: 18 }, branchRoot: { justifyContent: "center" }, header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, logo: { color: colors.onSurface, fontWeight: "900", fontSize: 20, letterSpacing: -0.5 }, logoAccent: { color: colors.brandPrimary }, location: { marginTop: 6, color: colors.muted, fontSize: 12 }, avatar: { width: 44, height: 44, borderRadius: 22, backgroundColor: colors.brandTertiary, alignItems: "center", justifyContent: "center" }, branchWrap: { marginTop: 18 }, greeting: { marginTop: 25, marginBottom: 18 }, eyebrow: { color: colors.brandPrimary, fontSize: 11, fontWeight: "900", letterSpacing: 1 }, headline: { color: colors.onSurface, fontSize: 30, fontWeight: "900", marginTop: 7, letterSpacing: -0.7 }, subline: { color: colors.muted, marginTop: 7, fontSize: 14 }, hero: { height: 265, borderRadius: 24, overflow: "hidden", marginBottom: 14 }, heroImage: { width: "100%", height: "100%", position: "absolute" }, heroOverlay: { flex: 1, padding: 20, justifyContent: "flex-end", backgroundColor: "rgba(10,10,10,0.55)" }, statusPill: { alignSelf: "flex-start", borderRadius: 20, paddingHorizontal: 10, paddingVertical: 6, flexDirection: "row", alignItems: "center", marginBottom: 22 }, statusDot: { width: 6, height: 6, borderRadius: 3, backgroundColor: colors.onBrandPrimary, marginRight: 6 }, statusText: { color: colors.onBrandPrimary, fontSize: 10, fontWeight: "900" }, heroTitle: { color: colors.onBrandPrimary, fontSize: 30, lineHeight: 31, fontWeight: "900" }, heroCopy: { color: colors.onBrandPrimary, fontSize: 12, marginTop: 8, maxWidth: 230, lineHeight: 18 }, heroButton: { flexDirection: "row", alignItems: "center", gap: 8, alignSelf: "flex-start", marginTop: 15, backgroundColor: colors.brandPrimary, paddingHorizontal: 15, paddingVertical: 11, borderRadius: 12 }, heroButtonText: { color: colors.onBrandPrimary, fontSize: 13, fontWeight: "800" }, closedCta: { alignSelf: "flex-start", flexDirection: "row", gap: 7, alignItems: "center", marginTop: 15, backgroundColor: colors.brandTertiary, paddingHorizontal: 12, paddingVertical: 11, borderRadius: 12 }, closedCtaText: { color: colors.onBrandTertiary, fontSize: 12, fontWeight: "900" }, infoRow: { backgroundColor: colors.surfaceSecondary, borderRadius: 16, padding: 15, flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 30 }, infoLabel: { color: colors.onSurfaceSecondary, fontSize: 12, fontWeight: "700" }, infoValue: { color: colors.muted, marginTop: 4, fontSize: 12 }, link: { color: colors.brandPrimary, fontSize: 12, fontWeight: "800" }, muted: { color: colors.muted, marginVertical: 10, fontSize: 12 }, horizontalCards: { gap: 12, paddingVertical: 18 }, updateCard: { width: 210, backgroundColor: colors.surfaceSecondary, borderRadius: 17, padding: 12, borderWidth: 1, borderColor: colors.border }, updateImage: { width: "100%", height: 85, borderRadius: 12, marginBottom: 10 }, updateAccent: { height: 85, borderRadius: 12, backgroundColor: colors.brandTertiary, alignItems: "center", justifyContent: "center", marginBottom: 10 }, updateTitle: { color: colors.onSurface, fontWeight: "900", fontSize: 14 }, updateText: { color: colors.muted, fontSize: 11, lineHeight: 16, marginTop: 5 }, noMenu: { alignItems: "center", paddingVertical: 18 }, categoryGrid: { flexDirection: "row", flexWrap: "wrap", justifyContent: "space-between", marginTop: 18 }, category: { width: "48%", flexDirection: "row", alignItems: "center", backgroundColor: colors.surfaceSecondary, borderRadius: 16, padding: 12, marginBottom: 12 }, categoryDisabled: { opacity: 0.55 }, categoryIcon: { width: 42, height: 42, borderRadius: 14, alignItems: "center", justifyContent: "center" }, categoryText: { color: colors.onSurface, fontSize: 14, fontWeight: "800", marginLeft: 10 } }));